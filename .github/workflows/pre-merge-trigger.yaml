name: Pre-merge trigger
on:
  workflow_dispatch: {}
  pull_request_target:
    types: [opened, synchronize, reopened, ready_for_review, labeled, unlabeled]
    branches: 
      - main
      - releases/**
jobs:
  execute_pre_merge:
    uses: bmyrcha/vllm-gaudi/.github/workflows/pre-merge.yml@{{github.event.pull_request.base.ref}}
    secrets: inherit
